[package]
name = "rzarrs"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib", "lib"]

[dependencies]
savvy = { version = "0.10.2", default-features = false }
zarrs = { version = "0.23", default-features = false, features = [
    "filesystem",
    "async",
    "ndarray",
    "gzip",
    "zstd",
    "blosc",
    "sharding",
    "transpose",
    "crc32c",
] }
zarrs_object_store = { version = "0.6", default-features = false }
url = "2"
tokio = { version = "1", default-features = false, features = ["rt", "time"] }
serde_json = "1"
flate2 = { version = "1", optional = true }

[features]
default = ["aws", "zip"]
http = ["zarrs_object_store/http"]
aws = ["http", "zarrs_object_store/aws"]
gcp = ["http", "zarrs_object_store/gcp"]
azure = ["http", "zarrs_object_store/azure"]
cloud = ["aws", "gcp", "azure"]
zip = ["dep:flate2"]
altrep = ["savvy/altrep"]
async-altrep = ["altrep", "tokio/rt-multi-thread", "tokio/sync"]
simd-cast = []

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
